import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "NeoTravel - Estimation de trajet",
  description: "NeoTravel est un service de réservation de trajets qui vous permet d'estimer le coût et la durée de votre voyage en quelques clics. Que vous planifiez un trajet en voiture, en train ou en avion, NeoTravel vous fournit des informations précises et fiables pour vous aider à prendre les meilleures décisions pour votre voyage.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col" suppressHydrationWarning>
        {children}
      </body>
    </html>
  );
}
